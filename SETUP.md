# 設定手順（スプレッドシート版）

作業時間の目安は15分ほどです。パソコンでの作業をおすすめします。

**おすすめ**：リマインドメールは設定した人の Gmail から送られます。青年委員用の Google アカウントを作り、そのアカウントで作業してください。

---

## 1. スプレッドシートを作る

1. [Google スプレッドシート](https://sheets.google.com) を開き、「空白のスプレッドシート」を作ります。
2. 名前を「青年委員 応援カレンダー」などに変えます。

## 2. Apps Script（GAS）を貼り付ける

1. スプレッドシートのメニューで **拡張機能 → Apps Script** を開きます。
2. 最初から入っている `function myFunction() {…}` をすべて消します。
3. このリポジトリの [`gas/Code.gs`](gas/Code.gs) の中身をすべてコピーして貼り付けます。
4. 上のほうにある `CONFIG` を確認します（あとで変更しても大丈夫です）。
   - `PAGE_URL`：公開ページのURL（手順5で決まります）。メール本文に載せます。
   - `REMINDER_HOUR`：リマインドを送る時刻。初期値は `18`（前日の18時台）です。
5. 💾（保存）を押します。
6. 左の ⚙️「プロジェクトの設定」で、タイムゾーンが「(GMT+09:00) 日本標準時」になっているか確認します。

## 3. シートを準備する

1. 画面上部の関数の選択欄で **`setup`** を選び、「実行」を押します。
2. 初回は「承認が必要です」と出るので、次の順に進めます。
   **権限を確認 → アカウントを選ぶ →「詳細」→「（安全ではないページ）に移動」→ 許可**
   （自分で作ったスクリプトなので、この表示が出るのは正常です）
3. スプレッドシートに「予定」「参加者」「応援メッセージ」の3つのシートができ、「予定」に9月・10月の6件が入ります。

## 4. ウェブアプリとして公開する

1. 右上の **デプロイ → 新しいデプロイ** を押します。
2. 「種類の選択」の ⚙️ で **ウェブアプリ** を選びます。
3. 次のように設定して「デプロイ」を押します。
   - 次のユーザーとして実行：**自分**
   - アクセスできるユーザー：**全員**
4. 表示された **ウェブアプリの URL**（`https://script.google.com/macros/s/…/exec`）をコピーします。

## 5. ページに URL を設定して公開する（GitHub）

1. GitHub のこのリポジトリで `config.js` を開き、✏️（編集）を押します。
2. `window.SEINEN_API_URL = "";` の `""` の中に、手順4の URL を貼り付けて保存（Commit）します。
3. **Settings → Pages** で、Source を「Deploy from a branch」、Branch を **`main` / `(root)`** にして Save します。
4. 1〜2分後、`https://（GitHubのユーザー名）.github.io/seinen-ouen/` でページが開けます。
5. このURLを、手順2の `PAGE_URL` に入れて保存します。
   Apps Script のコードを変えたときは、**デプロイ → デプロイを管理 → ✏️ → バージョン「新バージョン」→ デプロイ** で反映します（URL は変わりません）。

## 6. リマインドメールを有効にする

1. Apps Script の関数の選択欄で **`setupReminderTrigger`** を選び、「実行」を押します（承認を求められたら許可）。
2. 毎日 `REMINDER_HOUR` 時台に自動で動き、翌日の会の参加者でメールアドレスを入れた人にメールを送ります。
3. 送信の確認をしたいときは **`testReminderToMe`** を実行すると、自分宛てにテストメールが1通届きます。

---

## ふだんの運用

| やりたいこと | 方法 |
|---|---|
| 予定を追加する | 「予定」シートに1行追加（ID は空欄でOK。自動で入ります） |
| 予定を変更する | 「予定」シートのセルを書き換える |
| 講話者が複数 | 「講話者」のセル内で改行（Mac: ⌘+Enter／Windows: Alt+Enter）して1行に1人 |
| 肩書きを入れる | `氏名｜肩書き`（縦棒「｜」で区切る） |
| いたずら投稿を消す | 「参加者」「応援メッセージ」シートの該当行を削除 |
| 参加者の確認 | 「参加者」シートを見る（リマインド送信済みの日時も記録されます） |

- 日付は `2026-11-10` や `2026/11/10`、開始時刻は `06:00` や `6:00` の形で入力します。
- ページへの反映は最大30秒ほどかかります。
- 無料の Gmail アカウントでは、メールは1日100通までです。
